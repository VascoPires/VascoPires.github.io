version: "3"

services:
  jekyll_custom:
    build: .
    image: al-folio-local-docker
    container_name: al-folio-local-website
    command: bash -c "
      rm -f Gemfile.lock
      && bundler exec jekyll serve --watch --port=8081 --host=0.0.0.0 --verbose --trace"
    ports:
      - 8081:8081
    volumes:
      - .:/srv/jekyll
